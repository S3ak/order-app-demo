import styled from "styled-components/macro";

import ds from "../../../utils/styles/design-system";

export const Wrapper = styled.section`
  width: 100%;
  padding: 24px ${ds.get("layout.gutter")};
`;
