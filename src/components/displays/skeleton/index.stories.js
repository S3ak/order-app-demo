import React from "react";

import { Box, Circle } from "./styled";

export default {
  title: "Displays/Skeleton",
  component: Box,
};

export const Base = () => <Box />;
export const Round = () => <Circle />;
